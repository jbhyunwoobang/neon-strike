# Production build (GitHub Pages) connects multiplayer to the live Render server.
# Vite bakes this in at build time; local `npm run dev` ignores it and still
# uses localhost:8080. Change this if the server URL ever changes.
VITE_SERVER_URL=https://neon-strike-server.onrender.com
